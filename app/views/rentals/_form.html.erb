<%= form_with(model: rental, local: true) do |form| %>
  <% if rental.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rental.errors.count, "error") %> prohibited this rental from being saved:</h2>

      <ul>
      <% rental.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dateRented %>
    <%= form.date_select :dateRented, id: :rental_dateRented %>
  </div>

  <div class="field">
    <%= form.label :dateDue %>
    <%= form.date_select :dateDue, id: :rental_dateDue %>
  </div>

  <div class="field">
    <%= form.label :daysLeft %>
    <%= form.number_field :daysLeft, id: :rental_daysLeft %>
  </div>

  <div class="field">
    <%= form.label :returned %>
    <%= form.check_box :returned, id: :rental_returned %>
  </div>

  <div class="field">
    <%= form.label :dateReturned %>
    <%= form.date_select :dateReturned, id: :rental_dateReturned %>
  </div>

  <div class="field">
    <%= form.label :fee %>
    <%= form.text_field :fee, id: :rental_fee %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
